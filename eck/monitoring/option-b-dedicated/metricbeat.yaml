apiVersion: beat.k8s.elastic.co/v1beta1
kind: Beat
metadata:
  name: metricbeat-dedicated
  namespace: elastic-stack
spec:
  type: metricbeat
  version: ${ES_VERSION}
  # Send metrics to the dedicated monitoring cluster
  elasticsearchRef:
    name: ${MONITORING_CLUSTER_NAME}
  kibanaRef:
    name: ${MONITORING_CLUSTER_NAME}
  config:
    metricbeat.modules:
      - module: elasticsearch
        xpack.enabled: true
        period: 10s
        hosts:
          - "https://${ES_CLUSTER_NAME}-es-http.elastic-stack.svc:9200"
        username: "${MONITORING_USER}"
        password: "${MONITORING_PASSWORD}"
        ssl.verification_mode: "none"
        metricsets:
          - node
          - node_stats
          - cluster_stats
          - index
          - index_summary
          - shard
          - pending_tasks
      - module: kibana
        xpack.enabled: true
        period: 10s
        hosts:
          - "https://${ES_CLUSTER_NAME}-kb-http.elastic-stack.svc:5601"
        username: "${MONITORING_USER}"
        password: "${MONITORING_PASSWORD}"
        ssl.verification_mode: "none"
        metricsets:
          - stats
          - status
      - module: system
        period: 10s
        metricsets:
          - cpu
          - load
          - memory
          - network
          - process
          - process_summary
          - filesystem
          - fsstat
        process.include_top_n.by_cpu: 5
        process.include_top_n.by_memory: 5
  daemonSet:
    podTemplate:
      spec:
        serviceAccountName: metricbeat-dedicated
        automountServiceAccountToken: true
        tolerations:
          - key: "elastic-role"
            operator: "Exists"
            effect: "NoSchedule"
        containers:
          - name: metricbeat
            resources:
              requests:
                memory: 256Mi
                cpu: 100m
              limits:
                memory: 512Mi
        dnsPolicy: ClusterFirstWithHostNet
        hostNetwork: true
        securityContext:
          runAsUser: 0
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: metricbeat-dedicated
  namespace: elastic-stack
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metricbeat-dedicated
rules:
  - apiGroups: [""]
    resources: ["nodes", "namespaces", "events", "pods", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["extensions"]
    resources: ["replicasets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["statefulsets", "deployments", "replicasets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "watch"]
  - nonResourceURLs: ["/metrics"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: metricbeat-dedicated
subjects:
  - kind: ServiceAccount
    name: metricbeat-dedicated
    namespace: elastic-stack
roleRef:
  kind: ClusterRole
  name: metricbeat-dedicated
  apiGroup: rbac.authorization.k8s.io
